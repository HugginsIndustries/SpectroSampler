# Core dependencies
numpy>=1.24.0,<2.0.0
soundfile>=0.12.0
matplotlib>=3.7.0
tqdm>=4.65.0
typer[all]>=0.9.0

# Optional: webrtcvad (required for VAD detector)
# Note: On Windows, requires Visual C++ Build Tools or install pre-built wheel
# Install separately: pip install webrtcvad
# Or use: pip install -e ".[vad]" (if you have build tools)
# webrtcvad>=2.0.10

# Optional but recommended for development
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
ruff>=0.1.0
mypy>=1.5.0

